@using Ofqual.Common.RegisterFrontend.Models.APIModels;
@using Ofqual.Common.RegisterFrontend.Extensions;
@model Organisation
@{
    ViewData["Title"] = "Find a regulated awarding organisation";
    ViewData["HomePage"] = "find-regulated-organisations";
}

<div class="govuk-grid-row">
    <div class="govuk-grid-column-two-thirds-from-desktop">

        <h1 class="govuk-heading-xl">
            @Model.Name
        </h1>

        <a href="/qualifications?awardingOrganisations=@Model.Name&availability=Available to learners" class="govuk-button" data-module="govuk-button">Find all @Model.Acronym qualifications</a>

        @if (!(Model.Website == null && Model.ContactEmail == null))
        {
            <div class="govuk-summary-card">
                <div class="govuk-summary-card__title-wrapper"><h2 class="govuk-summary-card__title">Contact details</h2></div>

                <div class="govuk-summary-card__content">
                    <dl class="govuk-summary-list">
                        @if (Model.ContactEmail.IsNotNull())
                        {
                            <div class="govuk-summary-list__row">
                                <dt class="govuk-summary-list__key">
                                    Email
                                </dt>
                                <dd class="govuk-summary-list__value">
                                    <a href="mailto:@Model.ContactEmail">@Model.ContactEmail</a>
                                </dd>
                            </div>
                        }
                        @if (Model.Website.IsNotNull())
                        {
                            <div class="govuk-summary-list__row">
                                <dt class="govuk-summary-list__key">
                                    Website
                                </dt>
                                <dd class="govuk-summary-list__value">
                                    <a href="@Model.Website">@Model.LegalName</a>
                                </dd>
                            </div>
                        }
                        @if (Model.Website.IsNotNull())
                        {
                            <div class="govuk-summary-list__row">
                                <dt class="govuk-summary-list__key">
                                    Pricing and fees
                                </dt>
                                <dd class="govuk-summary-list__value">
                                    <a href="@Model.FeesUrl">Qualification fees and prices</a>
                                </dd>
                            </div>
                        }
                    </dl>
                </div>
            </div>
        }

        <div class="govuk-summary-card">
            <div class="govuk-summary-card__title-wrapper"><h2 class="govuk-summary-card__title">Ofqual recognition</h2></div>

            <div class="govuk-summary-card__content">
                <dl class="govuk-summary-list">

                    @if (Model.OfqualOrganisationStatus.IsNotNull())
                    {
                        <div class="govuk-summary-list__row">
                            <dt class="govuk-summary-list__key">
                                Status
                            </dt>
                            <dd class="govuk-summary-list__value">
                                @switch (Model.OfqualOrganisationStatus?.ToLower())
                                {
                                    case "surrendered":
                                        <strong class="govuk-tag govuk-tag--purple">
                                            Surrendered
                                        </strong>
                                        break;
                                    case "withdrawn":
                                        <strong class="govuk-tag govuk-tag--blue">
                                            Withdrawn
                                        </strong>
                                        break;
                                    case "recognised":
                                        <strong class="govuk-tag govuk-tag--green">
                                            Recognised
                                        </strong>
                                        break;
                                    case "never been recognised":
                                        <strong class="govuk-tag govuk-tag--grey">
                                            Never recognised
                                        </strong>
                                        break;
                                    default:
                                        break;
                                }
                            </dd>

                        </div>
                    }

                    @if (Model.OfqualRecognisedOn != null)
                    {
                        <div class="govuk-summary-list__row">
                            <dt class="govuk-summary-list__key">
                                Recognised from
                            </dt>
                            <dd class="govuk-summary-list__value">
                                <time datetime="@Model.OfqualRecognisedOn?.ToString("dd-MM-yyyy")" lang="en">@Model.OfqualRecognisedOn?.ToString("dd MMMM yyyy")</time>
                            </dd>

                        </div>
                    }


                    <div class="govuk-summary-list__row">
                        <dt class="govuk-summary-list__key">
                            Recognition number
                        </dt>
                        <dd class="govuk-summary-list__value">
                            @Model.RecognitionNumber
                        </dd>

                    </div>


                </dl>
            </div>
        </div>

        <div class="govuk-summary-card">
            <div class="govuk-summary-card__title-wrapper"><h2 class="govuk-summary-card__title">CCEA recognition</h2></div>

            <div class="govuk-summary-card__content">
                <dl class="govuk-summary-list">


                    @if (Model.CceaOrganisationStatus.IsNotNull())
                    {
                        <div class="govuk-summary-list__row">
                            <dt class="govuk-summary-list__key">
                                Status
                            </dt>
                            <dd class="govuk-summary-list__value">
                                @switch (Model.CceaOrganisationStatus?.ToLower())
                                {
                                    case "surrendered":
                                        <strong class="govuk-tag govuk-tag--purple">
                                            Surrendered
                                        </strong>
                                        break;
                                    case "withdrawn":
                                        <strong class="govuk-tag govuk-tag--blue">
                                            Withdrawn
                                        </strong>
                                        break;
                                    case "recognised":
                                        <strong class="govuk-tag govuk-tag--green">
                                            Recognised
                                        </strong>
                                        break;
                                    case "never been recognised":
                                        <strong class="govuk-tag govuk-tag--grey">
                                            Never recognised
                                        </strong>
                                        break;
                                    default:
                                        break;
                                }
                            </dd>

                        </div>
                    }


                    @if (Model.CceaOrganisationStatus != null)
                    {
                        <div class="govuk-summary-list__row">
                            <dt class="govuk-summary-list__key">
                                Recognised from
                            </dt>
                            <dd class="govuk-summary-list__value">
                                <time datetime="@Model.CceaRecognisedOn?.ToString("dd-MM-yyyy")" lang="en">@Model.CceaRecognisedOn?.ToString("dd MMMM yyyy")</time>
                            </dd>
                        </div>
                    }
                    <div class="govuk-summary-list__row">
                        <dt class="govuk-summary-list__key">
                            Recognition number
                        </dt>
                        <dd class="govuk-summary-list__value">
                            @Model.RecognitionNumber
                        </dd>
                    </div>

                </dl>
            </div>
        </div>

        <div class="govuk-summary-card">
            <div class="govuk-summary-card__title-wrapper"><h2 class="govuk-summary-card__title">Organisation details</h2></div>
            <div class="govuk-summary-card__content">
                <dl class="govuk-summary-list">
                    <div class="govuk-summary-list__row">
                        <dt class="govuk-summary-list__key">
                            Name
                        </dt>
                        <dd class="govuk-summary-list__value">
                            @Model.Name
                        </dd>
                    </div>
                    <div class="govuk-summary-list__row">
                        <dt class="govuk-summary-list__key">
                            Legal name
                        </dt>
                        <dd class="govuk-summary-list__value">
                            @Model.LegalName
                        </dd>
                    </div>
                    <div class="govuk-summary-list__row">
                        <dt class="govuk-summary-list__key">
                            Known as
                        </dt>
                        <dd class="govuk-summary-list__value">
                            @Model.Acronym
                        </dd>
                    </div>
                </dl>
            </div>
        </div>

        @if (Model.RecognitionScope != null && (Model.RecognitionScope.Inclusions.Any() || Model.RecognitionScope.Exclusions.Any()))
        {
            <!-- Display scope of recognition -->
            <div class="govuk-summary-card">
                <div class="govuk-summary-card__title-wrapper">
                    <h2 class="govuk-summary-card__title" id="sor">Scope of recognition</h2>
                </div>
                <div class="govuk-summary-card__content">
                    <div class="govuk-inset-text">
                        This expandable list shows the types of qualifications, and subject areas or apprenticeship standards that @Model.Name is recognised to offer as regulated qualifications, as well as qualifications that Pearson offers as unregulated qualifications.
                    </div>
                    @if (Model.RecognitionScope.Inclusions.Any())
                    {
                        <h3 id="sor-title" class="govuk-heading-s">@Model.Name is recognised to award the following regulated qualifications</h3>
                        <ul class="ofq-tree" id="scope-tree" role="tree" aria-labelledby="sor-title">

                            @foreach (var item in Model.RecognitionScope.Inclusions)
                            {
                                <li>
                                    <details>
                                        <summary class="govuk-body ofq-tree-parent-node" aria-expanded="false" aria-controls="@item.Type-list" role="treeitem">
                                            @item.Type
                                        </summary>
                                        <ul id="@item.Type-list">
                                            @foreach (var level in item.Levels)
                                            {
                                                <li>
                                                    <details>
                                                        <summary class="govuk-body ofq-tree-parent-node" aria-expanded="false" aria-controls="@item.Type-@level.Level-list" role="treeitem">
                                                            @level.Level
                                                        </summary>
                                                        @foreach (var recognition in level.Recognitions)
                                                        {
                                                            <ul id="@item.Type-@level.Level-list">
                                                                <li class="govuk-body"><div role="treeitem">@recognition</div></li>
                                                            </ul>
                                                        }
                                                    </details>
                                                </li>
                                            }

                                        </ul>
                                    </details>
                                </li>

                            }
                        </ul>
                    }
                    @if (Model.RecognitionScope.Exclusions.Any())
                    {
                        <h3 id="exclusion-title" class="govuk-heading-s">The following @Model.Name qualifications are not regulated</h3>
                        <ul class="ofq-tree" id="exclusion-tree" role="tree" aria-labelledby="exclusion-title">
                            <li>
                                <details>
                                    <summary class="govuk-body ofq-tree-parent-node" aria-expanded="false" aria-controls="x-Unregulatedqualifications-list" role="treeitem">
                                        Unregulated qualifications
                                    </summary>
                                    <ul id="x-Unregulatedqualifications-list">
                                        @foreach (var item in Model.RecognitionScope.Inclusions)
                                        {
                                            @foreach (var level in item.Levels)
                                            {
                                                @foreach (var recognition in level.Recognitions)
                                                {
                                                    <li class="govuk-body"><div role="treeitem">@recognition</div></li>
                                                }
                                            }
                                        }
                                    </ul>
                                </details>
                            </li>
                        </ul>
                    }

                    <a href="/Organisations/download-scopes-CSV/@Model.RecognitionNumber" role="button" draggable="false" class="govuk-button govuk-button--secondary" data-module="govuk-button">
                        Download scope of recognition CSV file
                    </a>

                </div>
            </div>
        }
    </div>
</div>